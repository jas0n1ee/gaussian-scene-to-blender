---
name: gaussian-scene-to-blender
description: 从空间 3D Gaussian Splatting 扫描重建可编辑的 Blender 原生建筑和家具，管理网页人工 Review，并按区域、LOD 和证据检查点续改。
---
## 开始与工具路线

使用用户项目为 PROJECT_ROOT。先读项目说明、当前检查点、布局/对象台账、验证结果及人工意见，确认实际最新 .blend；不要覆盖人工修改后的模型来重跑旧生成脚本。

优先用户指定的 Blender/3DGS 环境。官方正式版尚未通过项目兼容性验证时，使用已验证的 KIRI 3DGS Render 路线；0709A 验证组合为 Blender 5.2.1 LTS + KIRI 5.1.0，不等于其他版本或平台自动兼容。迁移官方路线前，在独立样板验证字段、坐标、同机位渲染及保存重开，不因版本发布直接替换工作环境。

## 坐标与命名

- 检查 PLY 字段，内存映射大文件；区别 SH DC/RGB、log-scale/尺度、opacity logit/alpha。原始文件不变，异常点过滤与裁剪只用于派生证据。
- 记录唯一的源到项目坐标变换、轴向、单位和尺度校准状态。可旋转整个场景使主墙方向平行 X/Y，保留真实斜墙；Z-up 数据不凭惯例翻转。所有楼层共享水平旋转、原点及垂直基准；如用楼层局部坐标，显式记录可逆变换。CAD、照片及额外扫描分别记录注册变换。
- 区分楼层分割与同层高度切片。BEV、立面、裁剪边界、模型采用同一项目坐标；标明 XYZ 范围，避免证据图未加 Z 偏移而模型已加。尺度未校核时不得宣称实测精度。
- R0、R1…表示累计模型修订版本，不对应对话、工具调用或渲染次数。
- 区域 ID 使用 N/S/E/W 及组合加数字，如 NW1、SW1；不便按方位标识的区域用 A1、A2。方向按固定 BEV 上 N、下 S、左 W、右 E，不代表测绘真北。多楼层加 F01、F02 等前缀。
- 房间与区域编号不加用途语义；ID 确定后保持稳定。已有项目保留稳定旧 ID，采用对照表解释，不强行重编号。对象仍保留 object_id、category、region、evidence、confidence 和人工修订记录。

## 证据与推进顺序

完成坐标检查、楼层分割和高度切片 BEV，再划分区域；在结构定案前逐区采集能互相补充的室内高斯透视。BEV 用于范围、连接和定位，不能独自确认实体墙/玻璃、柱体、门窗或椅子朝向。必要时主动生成立面以测门高、窗台、顶高；争议部位再补局部密集切片或投影。

先用有清晰墙、门和家具证据的小样板跑通导入、真实高斯渲染、原生模型及共用相机，再推进全层结构和主要家具。样板不作为提前细化大量小物件的理由。证据充分就复用已有机位；新机位用于解决具体遮挡、争议或人工复核，不按面积规定数量，也不为每次修订无差别重渲染全场。

## LOD 定义与阶段审计

以下是本流程的工作层级，不宣称对应 BIM 或其他行业 LOD 标准。几何精度和证据可信度不由层级数字保证；未指定目标时先完成结构与主要家具。各阶段留下累计文件、参数/对象清单、对照证据及未决项。人工 Review 按任务约定配置；不因未安排人工 Review 而省略阶段审计。

| 层级 | 产物范围 | 通过条件 |
| --- | --- | --- |
| LOD1 空间结构 | 每层轮廓与未扫描孔洞；独立墙、地面、门窗洞口、柱体；实际顶高、吊顶/裸顶及主要高差 | 区域连接和开口可通行，实体/透明类别明确；不虚构柱体或补填空白；争议结构明确留疑 |
| LOD2 主要布局 | 主要家具及设备的可辨认简模，数量、尺寸、位置和朝向；灯具及可见监控设备的准确位置、数量和方向；主要梁、风管、空调与管线轮廓 | 室内透视与切片支持布局；家具不越界，通道合理；实际椅背与坐面方向正确；灯具/监控不足证据处标为待核对 |
| LOD3 可编辑构件 | 家具和门窗拆成可独立编辑的必要构件；灯具外壳、发光面与光源；基础颜色、透明度和粗糙度 | 类别及主要外形可辨认；构件可局部修改，不影响无关对象；无明显穿墙、重叠面或封死门洞 |
| LOD4 选择性细节 | 按用途与可见证据补重要五金、边缘、接缝及必要设备细节 | 明确改善目标视角或后续使用；不把未知小物件、叶片、风口叶片及管线接头作为全场必做项 |
| LOD5 材质与外观 | 有尺度的纹理及基础色、粗糙度、法线/凹凸等材质信息，结合照明复核 | 不把扫描阴影、高光与伪影直接烘为基础色；近似材质和未校准颜色明确标注；适用时换光验证 |

基础颜色、实体/玻璃判断随结构与布局确认，不拖到最后。全层先满足较低层级再广泛细化；允许稳定样板提前试材质，并允许发现问题后回退修正。目标层级以下的关键错误优先于继续提高细节。

## 顶面、灯光与可编辑性

顶面可降低构件细节要求，但高度、吊顶/裸顶的几何、主要边界不能随意近似。灯具和监控设备按可见证据准确还原；本轮未充分核验的监控不能自动视为已完成。风管、空调、管线可简化内部和小构件，保留影响空间判断的外形、位置及净空。

灯具外壳、发光面和实际光源分离，提供共同开关以同步控制发光材质与光源能量，关灯仍保留外壳。环境光、展示补光与场景灯分组，复核时说明启用状态；未实测的灯功率不得解释为现场照度还原。

墙、门、窗、地面、顶面为独立原生对象，按区域集合组织；家具可用语义根对象组织可编辑构件。薄玻璃不能代替真实门洞。复杂地面采用保留孔洞的约束三角剖分，焊接后加厚。未扫描区域保持空白，不推测新增房间。

## 人工 Review 与交互工具

当用户要求准备人工 Review 或处理 Review 意见时，读取 [references/human-review.md](references/human-review.md)；实际启动、暂停、结束及发布结果时再读 [references/web-review-runtime.md](references/web-review-runtime.md)。审阅器代码随 Skill 位于 [scripts/web-review](scripts/web-review)，不要依赖某个项目目录中的 R33 专用启动脚本。Agent 负责核对审阅包、准备显示资产、启动本机服务并把实际 URL 发给 Human；Human 只负责在网页审阅和明确告知本轮完成。当前 MVP 不包含网页按钮自动唤醒 Agent；收到明确完成信号后由 Agent 结束本轮服务并继续处理。不要等待用户手工整理 Agent 已知的模型版本与路径。

当前目标工具为本机服务配合浏览器：左侧 Spark 显示完整扫描的流式 3DGS G，右侧 Three.js 显示从累计 `.blend` 导出的 GLB B。两侧在项目坐标中同步漫游；冻结机位后框选并填写意见。用户不需要关联模型对象，GLB 无需 `object_id`。人工复核可产生新机位，同机位可有多个问题，不按面积规定机位数量。保存干净双图和可复现的相机/标注 JSON；修改后按 Issue 展示 G、带反色框的修改前 B、修改后 B 三列图。

为网页审阅准备 B 时，从对应版本的原生模型集合导出完整 GLB，排除嵌入扫描参考；保留 `.blend` 为可编辑源，记录源/派生哈希、导出范围和 GLB 到项目坐标的变换。G 的流式派生物必须来自完整原始扫描，不用固定局部裁剪冒充完整漫游。旧 Blender add-on 已归档；已有固定机位网页也不能冒充交互审阅。随 Skill 提供的 GLB 导出器默认要求 `MODEL_NATIVE` 集合、可选的 `MODEL` View Layer 与 Blender +Z 项目坐标；其他项目先在独立副本核对和适配，不盲用默认变换。准备清单时如发现交互或渲染能力缺失，明确记录未就绪状态，不用空白图片或伪造渲染结果宣称准备完成。

## 参数、工具与文档

区域边界、相机、构件尺寸和摆放参数优先存于有版本的参数文件，生成工具读取它们；对象台账和验证报告不是完整重建参数。发生人工修改时先检查与参数源的差异并保留修改，不直接重新生成覆盖。需要户型图或程序化重建时，把结构参数与模型同步作为交付要求，不能用过期 layout.json 充数。

按职责整理工具：inspect/map/crop 解析、对齐、裁剪；measure/fit 测量与拟合；build/revise 生成与修改；render 渲染；verify/audit 验证；build_review 汇总对照。同类操作重复时考虑提炼通用工具，rollout 结束提出值得沉淀的复用项；一次性补丁归档，不将每个补丁都变成通用工具。

维护项目 doc/：goal/验收约定用于用户确认目标；checkpoint/progress/evidence 用于 Agent 续作和追溯；delivery/review 用于向用户呈递结果。区分当前状态与历史记录，当前检查点只指向实际最新产物。

## 停止条件与交付

达到目标层级的验收条件即交付；证据不足的细节列为待核对，不凭空细化。局部修订检查相关视角与无关对象是否保持；最终对照集来自同一交付模型，不混用历史版本。保留原始源链接，记录参考/顶面显示方式；抽样点云不能称为完整高斯渲染。

最终 .blend 保存后重新打开，在验证副本实际修改一个独立墙网格和门对象，保存重开确认修改保留且邻接对象未变；交付主文件保持正确几何。检查断路、家具越界、重叠面及误填空白。提供真实模型、参数/对象台账、全层预览、主要区域同机位对照和明确的未决项，不只交脚本。
